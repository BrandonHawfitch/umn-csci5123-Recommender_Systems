# replication

Output files are formatted as grouptype_numgroups_groupsize_utilityfunction_fairnessfunction_lambda_searchtype_recommendations_timestamp
Output file contents are formatted as a json dictionary where keys are a comma-delimited string of group member ids and values are movie ids composing the list of recommendations